import { SearchByAmoutPipe } from './search-by-amout.pipe';

describe('SearchByAmoutPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchByAmoutPipe();
    expect(pipe).toBeTruthy();
  });
});
